---
title: Dork
description: Uses search operators and terms on search engines to try gather more information
---

import NoiseLevel from "@site/src/components/NoiseLevel";

<NoiseLevel level="none" />

Uses search operators and terms on search engines to try gather more information

The search term currently is `site:<domain>` and will parse subdomains on the first page of Google only.

More search engines will be available soon, same for the information it will parse from the results.
