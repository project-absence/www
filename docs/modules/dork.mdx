---
title: Dork
description: Uses search operators and terms on search engines to gather more information.
---

import NoiseLevel from "@site/src/components/NoiseLevel";

<NoiseLevel level="none" />

Uses search operators and terms on search engines to gather more information.

It currently uses the following search patterns:

- `site:{domain}`; to find subdomains
- `"@{domain}"`; to find mails

## Configuration

| Parameter       | Description                                                                | Default  |
| --------------- | -------------------------------------------------------------------------- | -------- |
| `search_engine` | The search engine to use. Can be one of the following: `ecosia` & `google` | `google` |
